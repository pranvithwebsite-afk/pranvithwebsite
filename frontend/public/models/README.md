# Camera AI model

Place the production GLB at `frontend/public/models/camera.glb`.

The Camera AI widget detects the file before loading it. Until then it displays a lightweight camera-icon fallback.
